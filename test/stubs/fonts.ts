// `next/font/google` requires the Next.js compiler, so tests alias
// `@/app/ui/fonts` to these stubs instead.
export const inter = { className: 'inter' };
export const lusitana = { className: 'lusitana' };
